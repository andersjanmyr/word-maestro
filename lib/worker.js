// Generated by CoffeeScript 1.4.0
(function() {
  var messageHandler, wm;

  importScripts('../data/swedish-word-list.js', 'word-maestro.js');

  wm = new this.WordMaestro(WORDS, 'abcdefghijklmnoprstuvxyzåää', '138113231732312411143878444');

  messageHandler = function(e) {
    var matchingWords;
    matchingWords = [];
    if (e.data.cmd === 'findPermutedAndShortendWord') {
      matchingWords = wm.findPermutedAndShortendWord(e.data.pattern);
    } else {
      matchingWords = wm.findWord(e.data.pattern);
    }
    return postMessage({
      matchingWords: matchingWords
    });
  };

  addEventListener('message', messageHandler);

}).call(this);
